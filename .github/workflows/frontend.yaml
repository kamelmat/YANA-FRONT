name: Deploy React to GCP (DISABLED - Using Vercel)

# This workflow is disabled because we're using Vercel for automatic deployment
# GitHub Actions is not needed since Vercel handles the deployment automatically

on:
  # Commenting out all triggers to disable this workflow
  # push:
  #   branches:
  #     - main
  #     - cicd/pipelines
  # workflow_dispatch:
  
  # Using a manual trigger only to prevent "No event triggers defined" error
  workflow_dispatch:

jobs:
  disabled-notice:
    name: Workflow Disabled Notice
    runs-on: ubuntu-latest
    steps:
      - name: Notice
        run: |
          echo "=================================================="
          echo "🚨 This GitHub Actions workflow is DISABLED"
          echo "🚀 We use Vercel for automatic deployment instead"
          echo "✅ Your app deploys automatically via Vercel at:"
          echo "   https://yana-front.vercel.app"
          echo "=================================================="
          echo "To re-enable GCP deployment:"
          echo "1. Uncomment the push triggers above"
          echo "2. Configure GCP secrets in repository settings"
          echo "3. Remove this notice job"
          echo "=================================================="
